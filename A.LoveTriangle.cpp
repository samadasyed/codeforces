#include <bits/stdc++.h> 

using namespace std; 
int main () {
    int numOfTestCases; 
    cin >> numOfTestCases; 
    vector <int> sizes; 
    vector <int> enemyPositions; 
    vector<int>friendlypositions; 
    for (int i = 0; i < numOfTestCases; i++){
        cin>>sizes[i]>>enemyPositions[i]>>friendlypositions[i]; 
    }


    
    return 0; 
}